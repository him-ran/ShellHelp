# Linux-and-Windows-Linking-scripts
This repository contains the scripts which can be run on a Windows machine(JAVA Env) and can trigger/read contents of a linux shell script ot execute a shell script.
